{
  "name": "eduswap",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "dev": "",
    "format": "pnpm -C backend run format && pnpm -C backend run format",
    "lint": "pnpm -C backend run lint && pnpm -C backend run lint"
  },
  "devDependencies": {
    "husky": "^9.1.6"
  }
}
